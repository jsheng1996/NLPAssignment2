The code for FNNModel could be found in FNNMain.ipynb
To run the code, you could use jupyter notebook to access it.

model.pt is the FNNModel with the lowest perplexity metric
generated.txt is the word file generated by the model

The rest are the default python scripts provided by wordRNN example.